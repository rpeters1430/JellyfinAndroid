<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production configuration -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate store -->
            <certificates src="system"/>
            <!-- Trust user added certificate store -->
            <certificates src="user"/>
        </trust-anchors>
    </base-config>
    
    <!-- Allow cleartext HTTP for development/local servers -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust system certificate store -->
            <certificates src="system"/>
            <!-- Trust user added certificate store for testing -->
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>
    
    <!-- Configuration for local/development Jellyfin servers -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Common local IP ranges for development -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.0.0</domain>
        <domain includeSubdomains="false">192.168.0.0</domain>
        <domain includeSubdomains="false">172.16.0.0</domain>
        <!-- Add your local development domain here -->
        <!-- <domain includeSubdomains="true">your-local-jellyfin.local</domain> -->
    </domain-config>
    
    <!-- 
    For production Jellyfin servers with known certificates, add certificate pinning:
    
    <domain-config>
        <domain includeSubdomains="true">your-jellyfin-server.com</domain>
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>