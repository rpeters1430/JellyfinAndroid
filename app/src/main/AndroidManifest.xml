<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- For Android 13+ (API 33+) - granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Download manager access -->
    
    <!-- Video player permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Offline download permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Declare Android TV support -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".JellyfinApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JellyfinAndroid"
        android:networkSecurityConfig="@xml/network_security_config"
        android:banner="@drawable/tv_banner"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JellyfinAndroid"
            android:importantForAutofill="noExcludeDescendants"
            android:screenOrientation="user"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android TV Launcher Intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- App Shortcuts -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        
        <activity
            android:name=".ui.player.VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.JellyfinAndroid.VideoPlayer"
            android:launchMode="singleTask" />

        <!-- Picture-in-Picture action receiver -->
        <receiver
            android:name=".ui.player.PipActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rpeters.jellyfin.PIP_PLAY_PAUSE" />
                <action android:name="com.rpeters.jellyfin.PIP_SKIP_FORWARD" />
                <action android:name="com.rpeters.jellyfin.PIP_SKIP_BACKWARD" />
            </intent-filter>
        </receiver>

        <!-- Download action receiver -->
        <receiver
            android:name=".data.worker.DownloadActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rpeters.jellyfin.ACTION_PAUSE_DOWNLOAD" />
                <action android:name="com.rpeters.jellyfin.ACTION_RESUME_DOWNLOAD" />
                <action android:name="com.rpeters.jellyfin.ACTION_CANCEL_DOWNLOAD" />
            </intent-filter>
        </receiver>

        <!-- Cast support -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.rpeters.jellyfin.ui.player.CastOptionsProvider" />

        <service
            android:name=".ui.player.audio.AudioService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- WorkManager is initialized on-demand via Configuration.Provider in JellyfinApplication. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Required when workers call setForeground(ForegroundInfo(..., FOREGROUND_SERVICE_TYPE_DATA_SYNC)) -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
