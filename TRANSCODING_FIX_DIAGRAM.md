# Transcoding Position Reset Fix - Visual Flow

## Before Fix (Issue Present)

```
┌─────────────────────────────────────────────────────────────────┐
│  Video Playing at 116 seconds                                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  MediaRouter Event: onRestoreRoute()                            │
│  (Audio route temporarily disrupted)                            │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  ExoPlayer: STATE_BUFFERING                                     │
│  Current Position: 116408ms                                     │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Codec: FLUSHING → FLUSHED                                      │
│  (Video and Audio decoders cleared)                             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  ExoPlayer: STATE_READY                                         │
│  Current Position: 13ms  ❌                                     │
│  (POSITION LOST - RESET TO START!)                              │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Video plays from beginning                                     │
│  User frustrated - lost progress                                │
└─────────────────────────────────────────────────────────────────┘
```

## After Fix (Issue Resolved)

```
┌─────────────────────────────────────────────────────────────────┐
│  Video Playing at 116 seconds                                   │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  MediaRouter Callback: onRouteChanged()                         │
│  ✅ SAVE POSITION: 116408ms                                     │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  ExoPlayer: STATE_READY → STATE_BUFFERING                       │
│  ✅ SAVE POSITION AGAIN: 116408ms (double safety)               │
│  Set wasBufferingBeforeReady = true                             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Codec: FLUSHING → FLUSHED                                      │
│  (Video and Audio decoders cleared)                             │
│  savedPositionBeforeFlush = 116408ms 💾                         │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  ExoPlayer: STATE_BUFFERING → STATE_READY                       │
│  Current Position: 13ms (reset occurred)                        │
│                                                                  │
│  ✅ POSITION GUARD TRIGGERED:                                   │
│    - savedPos (116408ms) > 5000ms ✓                             │
│    - currentPos (13ms) < 5000ms ✓                               │
│    → RESTORE POSITION: seekTo(116408ms)                         │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Video continues from 116 seconds ✅                            │
│  User happy - no interruption                                   │
│  savedPositionBeforeFlush = null (cleanup)                      │
└─────────────────────────────────────────────────────────────────┘
```

## Position Save Points (Multiple Safety Nets)

```
╔═══════════════════════════════════════════════════════════════╗
║  Position Save Triggers                                       ║
╚═══════════════════════════════════════════════════════════════╝

1. MediaRouter.onRouteChanged()
   ├─ Bluetooth connect/disconnect
   ├─ Audio output change
   └─ Cast device connection

2. Player.onPlaybackStateChanged(BUFFERING)
   ├─ Network buffering
   ├─ Codec preparation
   └─ Stream rebuffering

3. Player.onPlayerError()
   ├─ Direct Play failure
   ├─ Codec error
   └─ Before transcoding fallback

4. releasePlayerImmediate()
   ├─ ViewModel cleanup
   ├─ Activity recreation
   └─ Before player re-init

5. retryWithTranscoding()
   ├─ Captures current position
   ├─ Uses max(current, saved)
   └─ Restores after new player ready
```

## Position Restore Logic

```kotlin
┌──────────────────────────────────────────────────────────┐
│ STATE_READY detected after buffering                     │
│                                                           │
│ IF savedPosition exists AND > 5000ms                     │
│ AND currentPosition < 5000ms                             │
│ THEN:                                                     │
│   ┌─────────────────────────────────────────┐           │
│   │ Log: "Detected position reset"          │           │
│   │ exoPlayer.seekTo(savedPosition)         │           │
│   │ Update UI state with saved position     │           │
│   │ Clear savedPosition                     │           │
│   └─────────────────────────────────────────┘           │
│ ELSE:                                                     │
│   ┌─────────────────────────────────────────┐           │
│   │ Log: "Position preserved correctly"     │           │
│   │ Clear savedPosition                     │           │
│   └─────────────────────────────────────────┘           │
└──────────────────────────────────────────────────────────┘
```

## Log Output Example

```
// Normal playback
VideoPlayer: State: READY, isPlaying: true
PlaybackProgressManager: Reported progress at 116408ms

// Route change detected
VideoPlayer: Media route changed: Phone
VideoPlayer: Saved position due to route change: 116408ms

// Buffering starts
VideoPlayer: State: BUFFERING, isPlaying: false
VideoPlayer: Saved position before potential flush: 116408ms

// Codecs flush (system logs)
CCodec: [c2.qti.avc.decoder] state->set(FLUSHING)
CCodec: [c2.qti.avc.decoder] state->set(FLUSHED)
CCodecBufferChannel: Discard frames from previous generation.

// Ready after flush
VideoPlayer: State: READY, isPlaying: true
VideoPlayer: Detected position reset (was 116408ms, now 13ms) - restoring position
VideoPlayer: Duration update: currentPosition=116408ms

// Continue playback
PlaybackProgressManager: Reported progress at 116408ms
PlaybackProgressManager: Reported progress at 121408ms
```

## Error Recovery Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  Direct Play @ 60 seconds                                       │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Audio Codec Error                                              │
│  ✅ onPlayerError: Save position (60000ms)                      │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  retryWithTranscoding() called                                  │
│  - Capture current: 60000ms                                     │
│  - Saved position: 60000ms                                      │
│  - Use max(60000, 60000) = 60000ms                              │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Release old player → New player with transcoding               │
│  ✅ seekTo(60000ms) after prepare()                             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Transcoded playback continues from 60 seconds ✅               │
└─────────────────────────────────────────────────────────────────┘
```

## Safety Features

✅ **5-second threshold**: Only restores if saved position > 5s
✅ **Reset detection**: Only restores if current position < 5s
✅ **Multiple save points**: Position saved at 5 different triggers
✅ **Max value logic**: Uses most recent position in fallback scenarios
✅ **State tracking**: Prevents false positives from legitimate seeks
✅ **Cleanup**: Clears saved position after successful restore
✅ **Logging**: All operations logged for debugging

## Performance Characteristics

- **Memory**: +40 bytes per instance (5 variables + 2 constants)
- **CPU**: Negligible (simple comparisons and assignments)
- **Network**: None (all local operations)
- **Latency**: <1ms for position save/restore
- **Side effects**: None (idempotent operations)

## Infinite Loop Issue (HLS Streams)

### Problem: Initial Fix Created Loop on HLS

```
┌─────────────────────────────────────────────────────────────────┐
│  Video at 116s on HLS transcoded stream                        │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  MediaRouter/Codec flush → Position resets to 13ms             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Fix detects reset: seekTo(116000ms)                           │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  ❌ HLS stream rebuffers on seek → STATE_BUFFERING              │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Codec flush again → Position resets to 13ms AGAIN             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Fix detects reset: seekTo(116000ms) ← LOOP FOREVER!           │
└─────────────────────────────────────────────────────────────────┘
```

### Solution: Retry Limits with Cooldown

```
┌─────────────────────────────────────────────────────────────────┐
│  Position reset detected (116s → 13ms)                         │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Check retry limits:                                            │
│  - attempts < 3? ✓                                              │
│  - cooldown > 2s? ✓                                             │
│  → Attempt 1: seekTo(116000ms)                                  │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  HLS rebuffers → reset again (13ms)                            │
│  → Attempt 2: seekTo(116000ms) [after 2s cooldown]             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  HLS rebuffers → reset again (13ms)                            │
│  → Attempt 3: seekTo(116000ms) [after 2s cooldown]             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  HLS rebuffers → reset again (13ms)                            │
│  Check retry limits:                                            │
│  - attempts < 3? ✗ (attempts = 3)                               │
│  ✅ ABORT: Log warning, reset counters, give up                │
│  "Position reset detected but restore limit reached"            │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  Playback continues from position 13ms                          │
│  No infinite loop - graceful degradation ✅                     │
└─────────────────────────────────────────────────────────────────┘
```

### Retry Logic

```
╔═══════════════════════════════════════════════════════════════╗
║  Position Restore Retry Logic                                ║
╚═══════════════════════════════════════════════════════════════╝

For each position reset detected:

  IF positionRestoreAttempts < maxRestoreAttempts (3)
  AND (currentTime - lastRestoreAttemptTime) > cooldownMs (2000)
  THEN:
    ├─ Increment positionRestoreAttempts
    ├─ Update lastRestoreAttemptTime = currentTime
    ├─ Log: "restoring position (attempt X/3)"
    └─ exoPlayer.seekTo(savedPosition)

  ELSE:
    ├─ Log: "restore limit reached - aborting"
    ├─ Reset positionRestoreAttempts = 0
    ├─ Reset lastRestoreAttemptTime = 0
    └─ Give up to prevent infinite loop

Reset counters when:
  - Position preserved correctly (currentPos >= 5000ms)
  - Starting new video playback (initializePlayer)
  - Giving up after hitting limits
```
